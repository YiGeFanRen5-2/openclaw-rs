[package]
name = "mcp-server"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
openclaw-core = { path = "../openclaw-core" }
clap = { version = "3.2", features = ["derive"] }

[dev-dependencies]
tempfile = "3.0"
